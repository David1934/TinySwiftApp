2. @2026/01/05 -- version update to 1.0.3_LM20260105A
    improve TinySwiftApp.
    http://gerrit.adaps.corp/c/SpadisQT/+/5607

    patch set 1: Do not Stop_streaming() and v4l2_close() until frame_process_thread is end;
    patch set 2: check the different environment variables to save frame rawdata/depth16/point-clound data;
    patch set 5: should use pix_mp instead of pix for V4L2_BUF_TYPE_VIDEO_CAPTURE_MPLANE in v4l2.cpp;
    patch set 6: do not dump callstack for CTRL-C;
    patch set 7: bugfix: PCM mode does not work(tree project);
    patch set 8: sync adaps_dtof_uapi.h with Linux kernel gerrit;
    patch set 10: update algo lib to 3.7.5;
    patch set 14: improve code to release some resource when exit the app;
    patch set 16: add EEPROM_PARAMETER_FETCH_DEMO code with build option EEPROM_PARAMETER_FETCH_DEMO;
                  sync code from SpadisQT, EEPROM data struct update 20251229;
    patch set 17: update swift algo lib to 3.7.6, rename MAX_CALIB_SRAM_ROTATION_GROUP_CNT to MAX_CALIB_SRAM_ROLLING_GROUP_CNT;


1. @2025/12/01 -- version update to 1.0.0_LM20251201A
    redmine #2832: SpadisQT极限精简到TinySwiftApp
    http://gerrit.adaps.corp/c/SpadisQT/+/5539

    patch set 1: delete some useless files;
    patch set 2: delete some useless code with some build options;
    patch set 3: add Makefile and CMakeLists.txt;
    patch set 4: remove QT related code;
    patch set 5: add DToF_Main class as main controller;
    patch set 6: first build pass version;
    patch set 7: first run-time pass version;
    patch set 8: add cmd line option handle for module_kernel_type and roi_sram_rolling;
    patch set 9: delete crc related functions, sync code change from SpadisQT;
    patch set 10: delete script-load and roisram-load related code in misc_device.cpp;
    patch set 11: update README.MD and release script file for TinySwiftApp;
    patch set 12: reduce more and update CMakeLists.txt file;
    patch set 13: add code to save decode output point-cloud buffer to binary files;
    patch set 14: enable algo lib dump data;
    patch set 16: add requirement_check() and improve command line handle;

